<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <CheckBox
        android:id="@+id/itemCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"/>
    <TextView
        android:id="@+id/itemTextviewName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Item"
        android:maxLines="1"
        android:layout_toStartOf="@id/itemLabelPriority"
        android:ellipsize="end"
        android:textColor="@color/black"
        android:layout_toEndOf="@id/itemCheckbox"
        android:layout_alignTop="@id/itemCheckbox"
        android:layout_alignBottom="@id/itemCheckbox"
        android:gravity="center_vertical"
        />

    <ImageView
        android:id="@+id/itemLabelPriority"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_priority"
        android:layout_alignTop="@id/itemCheckbox"
        android:layout_alignBottom="@id/itemCheckbox"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical"

        />


</RelativeLayout>